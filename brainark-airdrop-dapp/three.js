import { Canvas } from '@react-three/fiber';
import { Mesh } from 'three';

export default function MeshGradientBackground() {
  return (
    <Canvas>
      {/* Add your mesh and shader material here */}
    </Canvas>
  );
}